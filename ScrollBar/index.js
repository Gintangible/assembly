class ScrollBar {
    constructor(config) {
        this.el = config.el;
    }
    create(){
       
    }

}












var scrollBar = new ScrollBar({
    el: document.querySelector('.scroll-wrapper')
})